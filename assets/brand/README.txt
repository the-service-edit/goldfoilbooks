Reserved for Gold Foil Books logo files (SVG wordmark, favicon, foil texture)
when they are supplied. The prototype currently sets the wordmark in Fraunces
as live text, which keeps it crisp at every size and searchable by Google.
